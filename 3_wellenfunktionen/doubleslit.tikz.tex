\documentclass{standalone}
\input{../tikz_header}




\begin{document}

 
 
\begin{tikzpicture}
  \def\la{1}
  \def\phi{0.5}
  \def\dx{1.1}
  \def\a{-35}
  \def\b{-145}

  \begin{groupplot}
    [
        group style={
            group size=1 by 3,
            % columns=2,
            % rows=2,
          %  xlabels at=edge bottom,
          %  ylabels at=edge left,
            vertical sep= 3mm
        },
    ]

  \nextgroupplot[ axis equal,disabledatascaling, %scale only axis,
    font=\footnotesize,
      xmin=(-3), xmax=(3),
      ymin=(-4.4), ymax=(1.5),
      xtick=\empty,
      ytick=\empty,
      %axis lines=middle,
     % axis line style=thick,
     % tick style={black,thick},
     % ticklabel style={scale=0.8},
      %xlabel style={below=-1pt,font=\small},
      %ylabel style={above=-1pt},
      width=55mm, height=45mm,
      %tick scale binop=\times,
      %every y tick scale label/.style={at={(rel axis cs:0,1)},anchor=south}]
          ]

          \addplot[ blue] {\phi};
          \addplot[ blue] {\phi + \la};
          \addplot[ blue] {\phi + \la + \la};

          \tikzmath{\e = \la - \phi;}  
          \tikzmath{\f = 2*\la - \phi;}  
          \tikzmath{\g = 3*\la - \phi;}   
          \tikzmath{\h = 4*\la - \phi;}   
          \tikzmath{\j = 5*\la - \phi;}  
          \tikzmath{\k = 6*\la - \phi;}  

          \draw[blue] (\dx,0) ++ (\a:\e) arc (\a:\b:\e);
          \draw[blue] (\dx,0) ++ (\a:\f) arc (\a:\b:\f);
          \draw[blue] (\dx,0) ++ (\a:\g) arc (\a:\b:\g);
          \draw[blue] (\dx,0) ++ (\a:\h) arc (\a:\b:\h);
          \draw[blue] (\dx,0) ++ (\a:\j) arc (\a:\b:\j);
          \draw[blue] (\dx,0) ++ (\a:\k) arc (\a:\b:\k);

          \draw[blue] (-\dx,0) ++ (\a:\e) arc (\a:\b:\e);
          \draw[blue] (-\dx,0) ++ (\a:\f) arc (\a:\b:\f);
          \draw[blue] (-\dx,0) ++ (\a:\g) arc (\a:\b:\g);
          \draw[blue] (-\dx,0) ++ (\a:\h) arc (\a:\b:\h);
          \draw[blue] (-\dx,0) ++ (\a:\j) arc (\a:\b:\j);
          \draw[blue] (-\dx,0) ++ (\a:\k) arc (\a:\b:\k);


          \draw[line width=3pt, gray] (-5,0) -- (-\dx-0.2,0);
          \draw[line width=3pt, gray]  (-\dx+0.2,0) -- (\dx - 0.2, 0);
          \draw[line width=3pt, gray]  (\dx+0.2,0) -- (5, 0);
     
  \nextgroupplot[ 
          font=\footnotesize,
            xmin=(-4), xmax=(4),
            ymin=-1.3 , ymax=1.3,
          axis  x line =middle,    xtick=\empty,
          axis  y line =left,
          % axis line style=thick,
           % tick style={black,thick},
           % ticklabel style={scale=0.8},
            %xlabel style={below=-1pt,font=\small},
            %ylabel style={above=-1pt},
            width=55mm, height=25mm,
            %tick scale binop=\times,
            %every y tick scale label/.style={at={(rel axis cs:0,1)},anchor=south}]
                ]

                \addplot[ blue, samples=500, domain=-3.7:3.7] {cos(x * 360 * \la / (2 *\dx) )};


                     
  \nextgroupplot[ 
    font=\footnotesize,
      xmin=(-4), xmax=(4),
     ymin = 0, ymax=1.3,   
    axis  x line =middle,
    axis  y line =left,
    xtick=\empty,
    ytick = {0, 1},
    % axis line style=thick,
     % tick style={black,thick},
     % ticklabel style={scale=0.8},
      %xlabel style={below=-1pt,font=\small},
      %ylabel style={above=-1pt},
      width=55mm, height=25mm,
      %tick scale binop=\times,
      %every y tick scale label/.style={at={(rel axis cs:0,1)},anchor=south}]
          ]

          \addplot[ blue, samples=500,  domain=-3.7:3.7] {cos(x * 360 * \la / (2 *\dx) )^2};



     \end{groupplot}



\end{tikzpicture}



  


\end{document}


